# Backend API URL (server-side only, used by Next.js rewrites)
API_BASE_URL=http://localhost:8888/api

# Client-side API base (relative path, proxied through Next.js)
NEXT_PUBLIC_API_BASE_URL=/api

# WebSocket (direct connection to backend)
NEXT_PUBLIC_WS_URL=ws://localhost:8888/api/ws

# Auth Service
NEXT_PUBLIC_AUTH_URL=http://localhost:8100
NEXT_PUBLIC_AUTH_CLIENT_ID=app_xxx
